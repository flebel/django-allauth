###### TODO: this code should go into django-allauth

# from allauth.invitations.models import InvitationKey

# If invitation mode is on
# and if there is an invitation_key in the session
# get the invite by key
# key = InvitationKey.objects.get_key(invitation_key)
# make sure it is valid
# mark_used by user
#key.mark_used(user)
# user now has access
